#include <stdio.h>
/**
*main - Entry point
*
*Return: Always 0 (Success)
*/
int main(void)
{
	char Colly;

	for (Colly = 'a' ; Colly <= 'z' ; Colly++)
	{
		putchar(Colly);
	}
	{
		putchar('\n');
	}
	return (0);
}
