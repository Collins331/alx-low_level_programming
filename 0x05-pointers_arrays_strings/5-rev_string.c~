#include "main.h"
/**
*rev_string - entry point
*@s: the char type 
*/
void rev_string(char *s)
{
	int c;

	for (c = 0; s[c] != '\0'; c++)
		;

	for (c--; c >= 0; c--)

		_putchar(s[c]);

	_putchar('\n');
}
